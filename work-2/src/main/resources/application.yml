spring:
  datasource:
    url: jdbc:mysql://localhost/experiment1?useUnicode=true&characterEncoding=utf8&useSSL=false # 数据库连接地址
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 123456
  jpa:
    show-sql: true #打印sql语句
    database: mysql